# direnv: entering the directory loads the flake's default dev shell, so the
# tools and the theme variables the roles read are there without `nix develop`.
# Needs direnv and nix-direnv on the machine; `direnv allow` once per checkout
use flake
