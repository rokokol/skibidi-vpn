[defaults]
inventory = inventory/nodes.py
roles_path = roles
host_key_checking = True
# The community.general.yaml callback was removed in that collection's v12; the
# same output now comes from the built-in default callback with this option
result_format = yaml
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
